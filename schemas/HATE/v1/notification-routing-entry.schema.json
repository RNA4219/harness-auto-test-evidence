{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hate.local/schemas/HATE/v1/notification-routing-entry.schema.json",
  "title": "HATE Notification Routing Entry",
  "type": "object",
  "required": [
    "record_type",
    "record_id",
    "created_at",
    "decision_basis",
    "readiness_effect",
    "unsafe_output_policy",
    "operating_record_id",
    "subscriber_id",
    "routing_role",
    "delivery_target",
    "target_ref",
    "dedupe_key",
    "payload_hash",
    "redaction_report_ref",
    "sourceRefs"
  ],
  "properties": {
    "record_type": {"const": "notification-routing-entry"},
    "record_id": {"type": "string", "minLength": 1},
    "created_at": {"type": "string", "minLength": 1},
    "actor": {"type": "string", "minLength": 1},
    "system_actor": {"type": "string", "minLength": 1},
    "decision_basis": {"type": "array", "items": {"type": "string"}, "minItems": 1},
    "readiness_effect": {"type": "string", "enum": ["none", "hold"]},
    "unsafe_output_policy": {"type": "string", "minLength": 1},
    "operating_record_id": {"type": "string"},
    "subscriber_id": {"type": "string"},
    "routing_role": {"type": "string", "enum": ["primary", "escalation"]},
    "delivery_target": {"type": "string"},
    "target_ref": {"type": "string"},
    "dedupe_key": {"type": "string"},
    "payload_hash": {"type": "string"},
    "redaction_report_ref": {"type": "string"},
    "sourceRefs": {"type": "array", "items": {"type": "string"}, "minItems": 1}
  },
  "anyOf": [
    {"required": ["actor"]},
    {"required": ["system_actor"]}
  ],
  "additionalProperties": true
}
