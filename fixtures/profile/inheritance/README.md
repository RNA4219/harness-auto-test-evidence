# HATE-P1A-003 Profile Inheritance Fixture

This fixture documents the machine-readable profile inheritance contract shared
by P0a precheck and P1a trust evaluation.

The release profile must resolve through `default -> strict -> release`, expose
rule sources and rule diffs, and keep QEG Gate override fields false.
