# HATE-P1A-001 Adapter Registry Fixture

This fixture documents the adapter registry artifact emitted by P1a trust and
doctor commands.

The registry must list every implemented adapter manifest with capabilities,
input formats, output record types, fixtures, known limits, and profile support.
