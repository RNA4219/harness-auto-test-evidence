# HATE-P1B-001 RanD Requirements Packet Fixture

This fixture documents optional RanD `requirements_packet.json` ingest for P1b.
HATE must preserve upstream requirement verdicts and map KPI, acceptance, risk,
and source refs into `requirement-evidence-alignment.json`.
