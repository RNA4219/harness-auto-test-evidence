# HATE-P1A-005 Canonical Identity Fixture

This fixture documents canonical test identity hardening. The identity index must
split framework, file, classname, name, parameters, and matrix values. Matrix
values must not alter the logical canonical test id.
