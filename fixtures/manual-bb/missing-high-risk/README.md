# HATE-P0B-007 Manual BB Bridge Fixture

This fixture documents the manual-bb bridge contract emitted when a high-risk
changed path has no execution evidence.

The runtime source fixture is `fixtures/golden/p0b-qeg-minimal/input` with
`junit:tests/test_db.py::test_connection` removed from `HATE-test-results.ndjson`.
The expected shape is captured in `expected/manual-supplement-request.shape.json`.
