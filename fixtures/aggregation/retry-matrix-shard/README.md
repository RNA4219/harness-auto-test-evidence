# HATE-P1A-006 Retry / Matrix / Shard Aggregation Fixture

This fixture documents deterministic aggregation for retry, matrix, and shard
signals. Aggregation keys must use normalized canonical identity, matrix group,
and run attempt. Missing shard evidence must be visible as inconclusive.
