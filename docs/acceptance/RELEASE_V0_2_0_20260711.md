---
intent_id: INT-HATE-RELEASE-V0-2-0-20260711
owner: RNA4219
status: accepted
last_reviewed_at: 2026-07-11
next_review_due: 2026-07-25
verification_executed_at: 2026-07-11T12:16:33+09:00
verification_commit: b43376b2d28ff03317310201c395a5bcb1ee62b3
---

# HATE v0.2.0 Local Release Acceptance

## Scope

HATE v0.2.0のwheel配布、Draft 2020-12 Schema検証、local pluginの
fail-closed実行境界、Post-PoC状態台帳、CI/OSS公開整備を対象とする。
対象は上記commitへ本記録を含むworking-tree patchを適用した状態である。

このacceptanceはlocal evidence sliceの受入であり、公開、tag作成、
production readiness、外部QEG/release approvalを意味しない。

## Verification

- `uv run pytest -q` -> `1910 passed in 114.61s`
- `uv run mypy` -> strict対象3 moduleで成功
- 安全重要surfaceの `uv run ruff check ...` -> 成功
- `uv run python tools/ci/schema_meta_gate.py` -> error 0
- `uv run python tools/docs/render_post_poc_status.py --check` -> 成功
- `uv run python tools/ci/docs_freshness_gate.py` -> finding 0
- `uv build --out-dir C:\tmp\hate-v020-dist-20260711` -> sdist/wheel成功
- `uv run python tools/ci/wheel_smoke.py ...` -> repo外cwdの隔離venvで
  `hate --help`、P0a golden path、package schema load成功
- Windows Job Objectによる親子process cleanupとcrash findingを3回反復して成功

## Evidence

- wheel: `harness_auto_test_evidence-0.2.0-py3-none-any.whl`
- packaged schema: `hate/_schemas/HATE/v1`
- canonical status registry:
  `docs/process/post-poc-gap-registry.json`
- CI: static、Ubuntu 3.11/3.12/3.13 unit/contract、Windows process、
  package smoke、E2E/release evidenceの並列job
- plugin reportは実行可否、denial reason、強制済み/未強制control、
  外部署名証跡境界を保持する

## Open Risks

- `signature_valid`等は外部証跡の入力であり、暗号学的検証ではない。
- `subprocess_local`はfilesystem/network完全隔離とportable memory limitを提供しない。
- release/regulated profileではlocal subprocessを常時拒否し、
  containerized runner evidenceは別実装が必要である。
- GitHub ActionsのUbuntu/Windows matrixはローカルでは再現せず、
  remote CI成功が公開承認の前提として残る。
- `product_ready=false`であり、最終release authorityは外部にある。

## Decision

v0.2.0のlocal implementation/evidence sliceをacceptedとする。
公開、commit、tag、pushは未実施であり、全remote gate成功後の明示承認を要する。
